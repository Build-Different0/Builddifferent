
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const path=location.pathname.split('/').pop()||'index.html';$$('.nav a').forEach(a=>{if(a.getAttribute('href')===path)a.classList.add('active')});const toast=m=>{const t=$('#toast');if(!t)return;t.textContent=m;t.classList.add('show');clearTimeout(window.t);window.t=setTimeout(()=>t.classList.remove('show'),2200)};let audioOn=localStorage.getItem('parallel-sound')!=='off';const sb=$('#soundBtn');function sync(){if(!sb)return;sb.classList.toggle('on',audioOn);sb.querySelector('span').textContent=audioOn?'Sound on':'Sound off'}sync();sb?.addEventListener('click',()=>{audioOn=!audioOn;localStorage.setItem('parallel-sound',audioOn?'on':'off');sync();tone(620,.08)});function tone(f=440,d=.1,t='sine'){if(!audioOn)return;try{const c=new(window.AudioContext||window.webkitAudioContext)(),o=c.createOscillator(),g=c.createGain();o.type=t;o.frequency.value=f;g.gain.setValueAtTime(.035,c.currentTime);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+d);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+d)}catch(e){}}const canvas=$('#space');if(canvas){const c=canvas.getContext('2d');let s=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;s=Array.from({length:Math.min(220,Math.floor(innerWidth/6))},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.4+.2,v:Math.random()*.15+.03,a:Math.random()*.65+.2}))}function draw(){c.clearRect(0,0,canvas.width,canvas.height);for(const p of s){p.y+=p.v;if(p.y>canvas.height)p.y=0;c.beginPath();c.fillStyle=`rgba(255,255,255,${p.a})`;c.arc(p.x,p.y,p.r,0,Math.PI*2);c.fill()}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw()}
